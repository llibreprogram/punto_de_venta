DATABASE_URL="file:./dev.db"
# Configuración de formato y moneda
NEXT_PUBLIC_LOCALE=es-DO
NEXT_PUBLIC_CURRENCY=DOP
# Impuesto en % (por ejemplo 19 para 19%)
NEXT_PUBLIC_TAX_PCT=0
# URL base para SSR (opcional, e.g. http://localhost:3000)
NEXT_PUBLIC_BASE_URL=
# Datos del negocio (opcional)
NEXT_PUBLIC_BUSINESS_NAME=Mi Restaurante
NEXT_PUBLIC_TICKET_FOOTER=¡Gracias por su compra!
# Logo (URL pública o ruta en /public)
NEXT_PUBLIC_LOGO_URL=/logo.png

# Credenciales iniciales (seed):
# admin@local / admin123
# cajero@local / cajero123
# IMPORTANTE: Cambia estas claves tras el primer inicio de sesión en producción.

# Producción (recomendado):
# - Establece NODE_ENV=production al construir y ejecutar
# - Usa una base de datos persistente (SQLite file en volumen o Postgres)
# - Configura impresora de red
# NEXT_PUBLIC_BASE_URL=https://tu-dominio
